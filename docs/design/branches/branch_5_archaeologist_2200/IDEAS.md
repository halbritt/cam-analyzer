author: diverger-reviewer-1-001

# Archaeologist 2200 Ideas

- Excavation layers in every curve: future readers find raw cam-card facts, inferred events, measured traces, and dynamics models stacked as provenance strata behind one `CamProfile` surface.

- Interface-shaped negative space: all analysis modules accept lift functions and derivatives, so the missing cam-card vocabulary reveals that source churn was the ancient design hazard.

- Confidence rings like tree growth: each generated lift curve preserves measured-vs-inferred bands, letting later maintainers date which conclusions rested on sparse timing guesses.

- A reference shard in the museum drawer: the WR250R Web Cam 81-651 card survives as transcription, normalized model, approximate profile, and golden report fixture.

- Adapter toolmarks left visible: OCR boxes, CSV rows, Cam Doctor columns, and degree-wheel readings remain attached to normalized values instead of being polished away.

- Swap scars in the reports: replacing an inferred cam-card approximation with measured valve lift reruns identical analyses and records only the physical evidence that changed.